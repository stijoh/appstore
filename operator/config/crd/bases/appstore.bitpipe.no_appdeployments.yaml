---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: appdeployments.appstore.bitpipe.no
spec:
  group: appstore.bitpipe.no
  names:
    kind: AppDeployment
    listKind: AppDeploymentList
    plural: appdeployments
    singular: appdeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.appName
      name: App
      type: string
    - jsonPath: .spec.chartVersion
      name: Version
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.helmReleaseName
      name: Release
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AppDeployment is the Schema for the appdeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AppDeployment
            properties:
              appName:
                description: AppName is the name of the application from the catalog
                  (validated at runtime against available charts)
                minLength: 1
                type: string
              autoUpgrade:
                default: false
                description: AutoUpgrade enables automatic upgrades to new chart versions
                type: boolean
              chartVersion:
                description: ChartVersion is the specific chart version to deploy
                  (defaults to latest)
                type: string
              releaseName:
                description: ReleaseName is the Helm release name (auto-generated
                  if not specified)
                maxLength: 53
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              requestedBy:
                description: RequestedBy is the user ID who requested the deployment
                type: string
              suspend:
                default: false
                description: Suspend stops reconciliation of this deployment
                type: boolean
              teamId:
                description: TeamID identifies the team owning this deployment
                type: string
              values:
                description: Values are custom Helm values to override defaults
                x-kubernetes-preserve-unknown-fields: true
              valuesFrom:
                description: ValuesFrom references ConfigMaps/Secrets for values
                items:
                  description: ValuesReference references a ConfigMap or Secret for
                    Helm values
                  properties:
                    kind:
                      description: Kind of the values referent (ConfigMap or Secret)
                      enum:
                      - ConfigMap
                      - Secret
                      type: string
                    name:
                      description: Name of the referent
                      type: string
                    optional:
                      default: false
                      description: Optional marks this reference as optional
                      type: boolean
                    valuesKey:
                      default: values.yaml
                      description: ValuesKey is the key in the referent to read
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
            required:
            - appName
            - teamId
            type: object
          status:
            description: status defines the observed state of AppDeployment
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployedChartVersion:
                description: DeployedChartVersion is the currently deployed version
                type: string
              failureCount:
                description: FailureCount is the number of consecutive failures
                type: integer
              helmReleaseName:
                description: HelmReleaseName is the actual Helm release name
                type: string
              helmReleaseRevision:
                description: HelmReleaseRevision is the current revision
                type: integer
              lastAppliedValuesHash:
                description: LastAppliedValuesHash is a hash of the last applied values
                type: string
              lastAttemptedChartVersion:
                description: LastAttemptedChartVersion is the version last attempted
                type: string
              lastReconcileTime:
                description: LastReconcileTime is when reconciliation last occurred
                format: date-time
                type: string
              message:
                description: Message provides human-readable status information
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              phase:
                description: Phase is the current deployment phase
                enum:
                - Pending
                - Installing
                - Upgrading
                - Deployed
                - Failed
                - Uninstalling
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
